version: '3'
services:
  web:
    build:
      context: .
      target: ${TARGET:-development}
    ports:
      - '3000:3000'
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=${NODE_ENV:-development}
